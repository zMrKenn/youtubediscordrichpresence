# Rust build output
app/target/

# local config / secrets (the app id is baked in - these are only overrides)
.env
**/.env
.oauth-token.json

# packaged extensions (rebuild them with build-extensions.ps1 when publishing)
extension.zip
extension-firefox.zip

# OS junk
Thumbs.db
.DS_Store
